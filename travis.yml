sudo: required
services:
  - docker
before script: cd workflow/frontend/

before_install:
  - docker build -f workflow/frontend/Dockerfile.dev -t kspasov/trn-docker workflow/frontend/

script:
  - docker run -e CI=true kspasov/trn-docker npm run test -- -- coverage
